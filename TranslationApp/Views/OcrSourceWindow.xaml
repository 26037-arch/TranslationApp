<Window x:Class="TranslationApp.Views.OcrSourceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OCR Source" Width="680" Height="480" MinWidth="420" MinHeight="280" Background="#F8FAFC">
    <Grid Margin="14">
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
        <TextBlock Text="OCR Source" FontWeight="SemiBold" FontSize="15" Margin="0,0,0,9" />
        <TextBox x:Name="Editor" Grid.Row="1" AcceptsReturn="True" AcceptsTab="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                 SpellCheck.IsEnabled="True" Padding="10" TextChanged="Editor_OnTextChanged">
            <TextBox.InputBindings>
                <KeyBinding Key="Z" Modifiers="Control+Shift" Command="ApplicationCommands.Redo" />
            </TextBox.InputBindings>
        </TextBox>
        <TextBlock Grid.Row="2" Text="{Binding Status}" Foreground="#64748B" FontSize="12" Margin="0,8,0,0" />
    </Grid>
</Window>
